#include<iostream>
using namespace std;
int main(){

    // == -> check wheather two operands are equal or not
    int a = 5;
    int b = 6;
    cout<<boolalpha<<(a == b)<<endl; // equal to equal to
    cout<<boolalpha<<(a != b)<<endl; // Not equal to
    cout<<boolalpha<<(a < b)<<endl; // less than
    cout<<boolalpha<<(a > b)<<endl; // greater than
    cout<<boolalpha<<(a <= b)<<endl; // less than equal to
    cout<<boolalpha<<(a >= b)<<endl; // greater than equal to


    return 0;
}